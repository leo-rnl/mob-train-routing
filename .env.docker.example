# Docker Compose environment variables
# Copy this file to .env.docker and adjust values as needed
# Usage: docker compose --env-file .env.docker up -d

# PostgreSQL
POSTGRES_DB=mob_train
POSTGRES_USER=mob
POSTGRES_PASSWORD=mob_secret

# Backend
DB_PASSWORD=mob_secret
